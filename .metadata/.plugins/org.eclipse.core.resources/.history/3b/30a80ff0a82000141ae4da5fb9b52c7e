<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alimentador"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.alimentador.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <uses-permission android:name="android.permission.BLUETOOTH"/>
        </activity>
        <activity
            android:name="com.alimentador.Carregando"
            android:label="@string/title_activity_inicial" >
        </activity>
        <activity
            android:name="com.alimentador.HoraActivity"
            android:label="@string/title_activity_hora" >
        </activity>
        <activity
            android:name="com.alimentador.DataActivity"
            android:label="@string/title_activity_data" >
        </activity>
        
    </application>
	<uses-permission android:name="android.permission.BLUETOOTH"/>
</manifest>
